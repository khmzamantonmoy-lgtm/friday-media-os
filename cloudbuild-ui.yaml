steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
    [
      'build',
      '-t',
      'us-central1-docker.pkg.dev/$PROJECT_ID/media-pipeline/ui:latest',
      '-f',
      'Dockerfile.ui',
      '.'
    ]

images:
- 'us-central1-docker.pkg.dev/$PROJECT_ID/media-pipeline/ui:latest'
